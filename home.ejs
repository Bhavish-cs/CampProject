<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CampGrounds - Your Adventure Starts Here</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .hero-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .hero-content {
            z-index: 2;
            color: white;
            text-align: center;
        }

        .hero-title {
            font-size: 4rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            animation: fadeInUp 1s ease-out;
        }

        .hero-subtitle {
            font-size: 1.4rem;
            margin-bottom: 2rem;
            opacity: 0.95;
            animation: fadeInUp 1s ease-out 0.3s both;
        }

        .hero-description {
            font-size: 1.1rem;
            margin-bottom: 3rem;
            opacity: 0.9;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            animation: fadeInUp 1s ease-out 0.6s both;
        }

        .cta-buttons {
            animation: fadeInUp 1s ease-out 0.9s both;
        }

        .btn-hero {
            padding: 15px 40px;
            font-size: 1.2rem;
            font-weight: 600;
            border-radius: 50px;
            text-decoration: none;
            transition: all 0.3s ease;
            margin: 0 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

        .btn-primary-hero {
            background: linear-gradient(45deg, #28a745, #20c997);
            border: none;
            color: white;
        }

        .btn-primary-hero:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3);
            background: linear-gradient(45deg, #20c997, #28a745);
        }

        .btn-outline-hero {
            background: rgba(255, 255, 255, 0.1);
            border: 2px solid rgba(255, 255, 255, 0.8);
            color: white;
            backdrop-filter: blur(10px);
        }

        .btn-outline-hero:hover {
            background: rgba(255, 255, 255, 0.2);
            border-color: white;
            transform: translateY(-2px);
            box-shadow: 0 12px 30px rgba(255, 255, 255, 0.2);
        }

        .features {
            position: absolute;
            bottom: 50px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 40px;
            opacity: 0.8;
        }

        .feature {
            text-align: center;
            color: white;
        }

        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 10px;
            display: block;
        }

        .bg-shapes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
            overflow: hidden;
        }

        .shape {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
        }

        .shape1 {
            width: 200px;
            height: 200px;
            top: 10%;
            right: 10%;
            animation: float 6s ease-in-out infinite;
        }

        .shape2 {
            width: 150px;
            height: 150px;
            bottom: 20%;
            left: 15%;
            animation: float 4s ease-in-out infinite reverse;
        }

        .shape3 {
            width: 100px;
            height: 100px;
            top: 30%;
            left: 20%;
            animation: float 5s ease-in-out infinite;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-20px);
            }
        }

        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }

            .hero-subtitle {
                font-size: 1.2rem;
            }

            .btn-hero {
                padding: 12px 30px;
                font-size: 1rem;
                display: block;
                margin: 10px auto;
                max-width: 250px;
            }

            .features {
                flex-direction: column;
                gap: 20px;
                bottom: 30px;
            }
        }
    </style>
</head>

<body>
    <div class="hero-section">
        <!-- Background Shapes -->
        <div class="bg-shapes">
            <div class="shape shape1"></div>
            <div class="shape shape2"></div>
            <div class="shape shape3"></div>
        </div>

        <!-- Hero Content -->
        <div class="container hero-content">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <h1 class="hero-title">
                        <i class="bi bi-tree-fill"></i> CampGrounds
                    </h1>
                    <h2 class="hero-subtitle">Your Adventure Starts Here</h2>
                    <p class="hero-description">
                        Discover amazing camping spots, share your experiences, and connect with fellow adventurers.
                        Join our community of outdoor enthusiasts and find your next perfect camping destination.
                    </p>

                    <div class="cta-buttons">
                        <a href="/login" class="btn btn-primary-hero btn-hero">
                            <i class="bi bi-box-arrow-in-right me-2"></i>
                            Get Started
                        </a>
                        <a href="/register" class="btn btn-outline-hero btn-hero">
                            <i class="bi bi-person-plus me-2"></i>
                            Join Community
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Features -->
        <div class="features d-none d-md-flex">
            <div class="feature">
                <i class="bi bi-geo-alt-fill feature-icon"></i>
                <div>Discover Places</div>
            </div>
            <div class="feature">
                <i class="bi bi-camera-fill feature-icon"></i>
                <div>Share Photos</div>
            </div>
            <div class="feature">
                <i class="bi bi-star-fill feature-icon"></i>
                <div>Rate & Review</div>
            </div>
            <div class="feature">
                <i class="bi bi-people-fill feature-icon"></i>
                <div>Connect</div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>